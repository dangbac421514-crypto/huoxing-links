# Bundled polyfills contain significant whitespace inside JavaScript strings.
# Preserve the locked build output; whitespace lint still applies to all source.
admin/dist/assets/vendor-*.js -whitespace linguist-generated=true
