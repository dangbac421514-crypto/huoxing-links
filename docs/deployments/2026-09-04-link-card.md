# 2026-09-04 现有 Web 功能上线记录

用户确认部署到 `link.bjaajsdad.xyz`。最终核验时间：2026-09-04 22:37 UTC。

## 发布版本

- 后台：`https://link.bjaajsdad.xyz/web/#/login`
- 原版本：`ef0bc90a8aab1eb0931aee0fed3fff4ead43538c`
- 首轮候选：`cda323b1c71d2d593d1e3213e11674e589a94bb9`
- 最终应用版本：`35ea7590d128c5ed4b748f20adb9c9834851048f`
- 当前指针：`/www/wwwroot/link-card/current` 指向 `releases/35ea7590d128c5ed4b748f20adb9c9834851048f`
- 最终包：`link-saas-web-20260904-35ea759.tar.gz`
- 包 SHA-256：`471ef9cb5d02bc48e0359680d9b75c3ff2d1aff6e296110c5e5b04ffe631dea7`

包上传后校验压缩文件哈希及所有 `MANIFEST` 文件均通过。
最终应用 SHA 与随后仅更新本记录的文档提交区分，不把文档提交伪称为应用运行版本。

## 备份与数据保留

服务器私有备份目录：
`/www/wwwroot/link-card/shared/deploy-backups/20260904T220831Z-cda323b`

已备份数据库、旧代码、共享 storage 中的 app/certs、旧 env、Nginx 配置和 scheduler cron。
目录权限 0700；敏感文件 0600。数据库压缩检查通过，备份含 28 个 CREATE TABLE，
`MANIFEST` 校验通过。未把备份或凭据下载到本地、GitHub或公共目录。
未进行恢复演练，不声称恢复步骤已实测。

账号仍为 1 个，链接仍为 3 条；客诉渠道、工单、附件、投递记录仍为 0。
公共链路验收正常增加了两次访问统计，没有创建生产演示账号或测试工单。
保留共享 env、所有原密钥及 storage，不执行 `migrate:fresh`、生产 seed 或密码重置。
本次 `migrate --force` 显示 `Nothing to migrate`。

## 运行环境与服务

- PHP 8.3.33 / Laravel 13.29.0；Composer 锁与原版本完全一致。
- 使用站点私有的 Composer 2.10.3 验证/安装锁定生产依赖，没有替换系统旧 Composer。
  [官方下载及 SHA-256](https://getcomposer.org/download/) 已核对。
- MySQL、专用 Redis 和 storage 权限预检通过。Redis 使用既有 6380 端口、DB 5。
- env 只修改 `QUEUE_CONNECTION=sync` 为 `redis`；键级差异已核对，其他配置保留。
- 新增 `link-card-worker.service`，以 `www` 运行；active/running、enabled，检查时 NRestarts=0。
  实际 unit 与仓库 `deploy/systemd/link-card-worker.service` 哈希一致。
- 保留 `/etc/cron.d/link-card-scheduler` 的每分钟调度；crond active，未新增第二个 scheduler。
- 只对本站 PHP 8.3 master 发送平滑重载信号。Nginx 配置未改，其他站点未重启。
- 系统 PHP 保留原有 `Module zip is already loaded` 警告；未扩大到全局 PHP 配置修复。

## 线上验收与补丁

首轮上线核验发现原有三个链接均处于 health_status=0，但旧 UI 仍按 status=1 显示可用。
其中两条 H5 卡片 QR 配置实际健康，仅因为没有小程序 min_id 被旧 checker 错误停用。

因此补充并部署了最终修复：H5 健康检查与既有 resolver 分支一致；
六类列表采用 API 已有的 effective_status。小程序引用非法、QR 过期和空配置仍拒绝。
不修改人工开关，不直接强制健康状态。

| 检查 | 结果 |
| --- | --- |
| 最终本地完整 PHPUnit | 423 tests / 2528 assertions，通过 |
| 最终正式构建浏览器验收 | 9 passed / 18.0s，含停用状态回归 |
| 部署工具行为测试 | 20 checks，通过 |
| 公开后台 index、config.js | HTTP 200，内容与发布文件一致 |
| 用户现有登录态 | 保留，真实 Chrome 可进入 admin-home |
| 客诉工作台 | 可打开，渠道与工单为空符合数据库状态 |
| 两条 H5 链接（ID 2、3） | 目标 API 200/code=0，二维码页 200，二维码数据 200/code=0，图片 200 且能解析为图片 |
| 重复健康检查 | 两条 H5 均 checked=1 failed=0；人工开关仍为 1 |
| Redis worker / cron | active；failed_jobs=0；cron 原配置保留 |

## 仍需运营配置

- ID 1 的企业微信目标是 `https://work.weixin.qq.com/help`，并非实际获客链接；
  检查结果 `WORK_WECHAT_HEALTH_FAILED` 被保留，需要运营方填写真实目标。
- 客诉受理尚无渠道，需在后台新建自己的商家渠道后才能对外分发链接。
- 没有配置实际群机器人，也没有发送真实企微通知；worker 运行不等于群消息已送达。
- 本轮不证明微信/抖音客户端唤起与加好友成功。
- 原有两项 moderate、一项 low 的前端依赖审计结果及此前一次未再复现的开发启动瞬态，
  继续保留在 `docs/ready-to-use-acceptance.md`，未静默删除。
